const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const mediaRecord = await prisma.businessProfileMedia.findFirst({ where: { businessProfileId: 1, name: { equals: 'eertqaa', mode: 'insensitive' }, isActive: true, deletedAt: null }, include: { syncs: { where: { platform: 'whatsapp', identifier: '1105381092649827' } } } }); console.log(JSON.stringify(mediaRecord, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
